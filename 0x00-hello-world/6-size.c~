#include <stdio.h>

/**
 * main - print with proper grammar, but the output is a piece of art,
 * 
 * Return - Always 0 (no error exit code)
*/

int main (void) {
  
  printf("with proper grammar, but the outcome is a piece of art,");
  
  return 0;

}

